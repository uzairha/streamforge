version: v2
modules:
  - path: proto
lint:
  use:
    - STANDARD
  except:
    # StreamEvents returns a bare `stream ChainEvent`, not a wrapper message.
    - RPC_RESPONSE_STANDARD_NAME
breaking:
  use:
    - FILE
